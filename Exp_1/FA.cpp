// Sample Program-1: Create a program in Python that implements a Finite Automata to validate strings that start with 'a' and end with 'b'.

#include<iostream>
#include<string>

using namespace std;

bool Finite_automata(string s) {
    int n = s.length();
    string start_state = "q0";
    string start_state = "q3";
    string current_state = "q0";

    
}

int main() {
    return 0;
}